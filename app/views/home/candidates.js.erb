$("#candidates").html("<%= j (render 'candidates', candidate: @candidates) %>");
$("#talk_push_candidates").html("<%= j (render 'talk_push_candidates', talk_push_candidates: @talk_push_candidates) %>");
